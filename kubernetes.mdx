---
title: 'Running Subtrace on Kubernetes'
description: 'Trace all requests within your Kubernetes clusters, pods and deployment.'
---

TODO: details

```
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: kubedemo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubedemo
  template:
    metadata:
      labels:
        app: kubedmeo
    spec:
      containers:
      - name: node
        image: adtac/kubedemo
        command: ["bash"]
        args: ["-c", "subtrace run -log=true -- node /server.js"]
```
